/// Shared constants for Purple Flutter (Phase 0 scaffold).
class AppConstants {
  AppConstants._();

  static const appName = 'Purple';

  /// Matches production Capacitor bundle (`org.purplelife.app`).
  static const iosBundleId = 'org.purplelife.app';
  static const androidApplicationId = 'org.purplelife.app';
}
