Visually verify the marketing pages on the live site to confirm no other hero-image issues:

1. Screenshot `/`, `/about`, `/community`, `/contact`, `/features` on production.
2. If any image is missing or broken, fix the affected route the same way as `/sign-in` (remove opaque backgrounds covering image layers, or repair the picture/source setup).
3. Report back with what passed and what (if anything) needed fixing.