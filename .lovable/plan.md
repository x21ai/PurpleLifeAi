## Plan

1. **Fix the broken production image URLs**
   - Remove the fragile `.jpeg → .jpg` rewrite in `ResponsiveImage` that is likely turning valid generated image URLs into 404s on the live site.
   - Keep the existing AVIF/WebP/JPG `srcset` behavior so browsers still choose optimized formats.

2. **Make the sign-in image failure visible and resilient**
   - Add an `onError` fallback state to `ResponsiveImage` so pages do not silently render a black/empty hero if an image fails.
   - Keep the sign-in page readable with the existing dark overlay, but avoid making it look like the photo is intentionally absent.

3. **Check every place using the same image component**
   - Verify `/sign-in`, `/features`, and the marketing routes that use `CalmScene` (`/`, `/about`, `/community`, `/contact`) because they share the same image pipeline.

4. **Validate production behavior**
   - Inspect the live network requests for the sign-in hero image.
   - Confirm the image loads on desktop and a mobile viewport after the fix is applied and published.

## Technical notes

- The affected code is `src/components/marketing/responsive-image.tsx` and `src/lib/calm-images.ts` imports generated by `vite-imagetools`.
- The uploaded screenshot is a reference only; I will not embed it into the app.